database:
  host: "localhost"
  port: "5432"
  user: "bankie_app"
  dbname: "bankie_main"
  # Optional read replica — omit to use primary for all reads
  # read_replica:
  #   host: "localhost"
  #   port: "5433"
  #   user: "bankie_app"
  #   dbname: "bankie_main"
redis:
  host: "localhost"
  port: "6379"
